<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:mm="clr-namespace:MediaManager.Forms;assembly=MediaManager.Forms"
             x:Class="App1.Vid">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="27"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Frame BackgroundColor="#FFB800"
                       CornerRadius="12" Grid.Column="0" Grid.Row="0" Margin="5,0,0,-20 ">

            <Label Text="{Binding name[0..10]}"
                               FontSize="Small"
                               TextColor="White"
                               HorizontalOptions="Center"
                               VerticalOptions="StartAndExpand"
                               Margin="0,-12,0,0"/>

        </Frame>
        <mm:VideoView Source="{Binding url}" 
                       ShowControls="True"
                       AutoPlay="False"
                       VerticalOptions="FillAndExpand"
                       Grid.ColumnSpan="2"
                       Grid.Row="1"
                       HeightRequest="200"
                       Margin="5,0,5,10"></mm:VideoView>
    </Grid>
</ContentView>